[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pytest = ">=8,<9"
pytest-cov = ">=5,<6"

[packages]
flask = ">=3.0,<4"
python-dotenv = ">=1.0,<2"
SQLAlchemy = ">=2.0,<3"
marshmallow = ">=3.21,<4"
marshmallow-enum = ">=1.5,<2"
flask-marshmallow = ">=0.15,<0.16"
psycopg2-binary = ">=2.9,<3"  # << para Postgres

[requires]
python_version = "3.11"  # en Docker usaremos python:3.11-slim
